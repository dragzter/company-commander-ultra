import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.companycommander.ultra",
  appName: "Company Commander",
  webDir: "dist",
};

export default config;
