{
  "name": "company-commander-ultra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:skip-types": "vite build",
    "deploy": "npm run build:skip-types && vercel --prod",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open": "npx cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "eslint": "^9.26.0",
    "globals": "^16.1.0",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.32.0",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@faker-js/faker": "^9.8.0",
    "prettier": "^3.5.3",
    "uuid": "^11.1.0",
    "zustand": "^5.0.4"
  }
}
